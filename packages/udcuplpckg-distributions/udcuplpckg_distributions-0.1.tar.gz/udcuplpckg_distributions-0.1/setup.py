from setuptools import setup

setup(name='udcuplpckg_distributions',
      version='0.1',
      description='Gaussian distributions',
      packages=['udcuplpckg_distributions'],
      zip_safe=False)
