[tool.poetry]
name = "{{project_name}}"
version = "0.1.0"
description = ""
authors = ["{{author}}"]
readme = "README.{{readme_ext}}"

[tool.poetry.dependencies]
python = "{{python_ver}}"


[build-system]
requires = ["{{build_system_req}}"]
build-backend = "poetry.core.masonry.api"
