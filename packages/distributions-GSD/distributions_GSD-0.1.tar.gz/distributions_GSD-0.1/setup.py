from setuptools import setup

setup(name='distributions_GSD',
      version='0.1',
      description='Gaussian and Binomial distributions',
      packages=['distributions_GSD'],
      author='Deepa B',
      zip_safe=False)
