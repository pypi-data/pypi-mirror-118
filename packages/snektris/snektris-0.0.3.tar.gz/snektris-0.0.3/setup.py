#!/usr/bin/env python
# setup.py generated by flit for tools that don't yet use PEP 517

from distutils.core import setup

packages = \
['snektris']

package_data = \
{'': ['*']}

install_requires = \
['pygame']

entry_points = \
{'console_scripts': ['snek_net = snektris.network:cli',
                     'snektris = snektris.snektris:cli']}

setup(name='snektris',
      version='0.0.3',
      description='SNEKTRIS',
      author='Gabriel Kabbe',
      author_email='gabriel.kabbe@mail.de',
      url='https://github.com/gab50000/snektris',
      packages=packages,
      package_data=package_data,
      install_requires=install_requires,
      entry_points=entry_points,
     )
