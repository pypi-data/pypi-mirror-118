from setuptools import setup

setup(name='distributions_learn',
      version='0.1',
      description='Gaussian and Binomial distributions',
      packages=['distributions_learn'],
      author="Nikhil Jose",
      zip_safe=False)
