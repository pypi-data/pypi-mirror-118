from setuptools import setup

setup(name='dnaa_distributions',
      version='0.1',
      description='Gaussian and Binomial distributions',
      packages=['dnaa_distributions'],
      zip_safe=False)
