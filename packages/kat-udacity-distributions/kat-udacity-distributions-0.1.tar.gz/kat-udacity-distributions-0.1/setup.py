from setuptools import setup

setup(name='kat-udacity-distributions',
      version='0.1',
      description='Gaussian distributions',
      packages=['kat-udacity-distributions'],
      zip_safe=False)
