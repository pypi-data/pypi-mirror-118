from setuptools import setup

setup(name='gauss_binom_distributions_for_ds',
      version='0.1',
      description='Gaussian and Binomial distributions',
      packages=['distributions'],
      zip_safe=False)
